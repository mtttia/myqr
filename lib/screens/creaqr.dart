import 'package:flutter/material.dart';

class CreateQR extends StatelessWidget {
  const CreateQR({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('crea un qr')),
    );
  }
}