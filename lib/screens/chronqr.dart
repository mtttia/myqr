import 'package:flutter/material.dart';

class ChronQR extends StatelessWidget {
  const ChronQR({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('cronologia')),
    );
  }
}